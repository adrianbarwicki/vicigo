<div class="navbar navbar-fixed-top navbar-default">
   <div class="container">


 				<div class="navbar-header">
         <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
         </button>
         <a class="navbar-brand" href="/" target="_self"><img class="header-logo" src="/img/vicigo_logo_short_dark_size_1.png" alt="Vicigo Logo"></a>
					<form class="navbar-form pull-left" role="search" method="get" >
					<div class="input-group">
						<input type="text" name="hashtag" class="form-control" placeholder="#search">
						<div class="input-group-btn">
							<button type="submit" class="btn btn-default"><span class="fa fa-search"></span></button>
						</div>
					</div>
				</form>
      </div>

		
		<div class="navbar-collapse collapse">
         <ul class="nav navbar-nav navbar-right">
       <% if (user) { %>

            <li>
							<a href="/profile/<%- user.id %>" target="_self">
								<img src="<%- user.profile_image_url %>" alt="profile pic" class="img-circle img-thumbnail-avatar-smallest">
							</a>
					 </li>
					 
					 	<% if (post.post_status == 0) { %>
				<li style="padding: 5px; padding-top: 13px;">
					<span id="saveDraftBtn" class="btn btn-info pull-left" onclick="editor.saveDraft()"><strong>Save draft</strong></button>
				</li>
				<% } %>
				<% if (post.post_status == 2) { %>
				<li style="padding: 5px; padding-top: 13px;">
						<span id="unpublishBtn" class="btn btn-info pull-left" onclick="editor.unpublish()"><strong>Unpublish</strong></button>
				</li>
				<% } %>
				<li style="padding: 5px; padding-top: 13px;">
						<span id="updateBtn" class="btn btn-info pull-left" onclick="<% if (config.editMode) { %>editor.update()<% } else { %>editor.publish()<% } %>"><strong><% if (config.editMode) { %>Update<% } else { %>Publish<% } %></strong></button>
				</li>
					 
           <li>
							<a href="/settings" target="_self">
								<i class="gi gi-settings fa-fw pull-right"></i> settings
							</a>
						</li>

						<li>
							<a href="/logout" target="_self">
								<i class="fa fa-power-off fa-fw pull-right"></i> logout 
							</a>
						</li>

				
				
      <% } else { %>
				<li><a href="/login" target="_self">Login</a></li>
				<li><a href="/signup" target="_self">Sign up</a></li>
      <% } %>
         </ul>
    </div>


   </div>
</div>